apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
        }
        CHEN {
            keyAlias 'weixin'
            keyPassword '2014+-1995cwx'
            storeFile file('F:/Android document')
            storePassword '2014+-1995cwx'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.1"
    defaultConfig {
        applicationId "c07140511xin.gdmec.edu.cn.work7"
        minSdkVersion 19
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        debug {
            signingConfig signingConfigs.CHEN
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:23.0.1'
    compile files('libs/BaiduLBS_Android.jar')
}
